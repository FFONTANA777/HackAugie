/*
MINIGAMES:
Each key maps a minigame id to its initializer function.
The initializer receives the encounter definition and the gameObject.
Returns a result object with a description for the UI to display.
*/

export const MINIGAMES = {
    // rarity_guess: initRarityGuess,
}